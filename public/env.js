// Runtime config placeholder. In production the container entrypoint overwrites
// this file from the INTUITION_NETWORK env var. Left empty here so dev and any
// build falls back to the build-time VITE_INTUITION_NETWORK.
window.__OG__ = {}
